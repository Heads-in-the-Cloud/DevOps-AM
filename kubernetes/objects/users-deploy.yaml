apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-users-deploy
  labels:
    app: api-users-app
    group: utopia
spec:
  selector:
    matchLabels:
      app: api-users-app
  replicas: $EKS_REPLICA_COUNT
  template:
    metadata:
      labels:
        app: api-users-app
        group: utopia
    spec:
      containers:
        - name: api-users-app
          image: $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION_ID.amazonaws.com/am-users-api:$USERS_API_LATEST
          imagePullPolicy: Always
          resources:
            limits:
              cpu: "$EKS_CONTAINER_CPU_LIMIT"
              memory: "$EKS_CONTAINER_MEM_LIMIT"
            requests:
              cpu: "$EKS_CONTAINER_CPU_REQUEST"
              memory: "$EKS_CONTAINER_MEM_REQUEST"
          ports:
            - containerPort: 8083
          envFrom:
            - secretRef:
                name: utopia-secret
---
apiVersion: v1
kind: Service
metadata:
  name: api-users-service
  labels:
    app: api-users-app
spec:
  ports:
    - protocol: TCP
      port: 8083
      name: http
  selector:
    app: api-users-app
